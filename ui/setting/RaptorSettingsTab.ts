export enum RaptorSettingsTab {

    /* Sidebar options */

    GENRAL = "GENERAL",
    EXTRA = "EXTRA",
    PLUGIN = "PLUGIN",

    /* Special options */

    PINNED = "PINNED", //shouldn't be used for requested areas
    MENU = "MENU", //accessed by the cog button
    NONE = "NONE" //shouldn't be used

}